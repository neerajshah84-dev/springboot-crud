
 
 spring:
   datasource:
     url: jdbc:postgresql://localhost:5432/springboot-crud?createDatabaseIfNotExist=true&autoReconnect=true&showSSL=false
     username: postgres
     password: postgres

   jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
     properties:
       hibernate:
         dialect: org.hibernate.dialect.PostgreSQLDialect
